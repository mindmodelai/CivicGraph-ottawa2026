# Kiro-CLI-RDP Progress

Timestamp: 2026-04-29T15:03:44Z

## Done

- **Task 2 (infra provisioning):** Neptune Serverless cluster `civicgraph-graph` (1–8 NCU), staging S3 bucket, IAM roles (neptune-loader + lambda-execution), security groups, DB subnet group, 4 CloudWatch log groups, CDK bootstrap. All resources tagged `Project=civicgraph`. Full report in `infra/provisioning-status.md`.
- **Project scaffold:** Repo structure (`apps/web/`, `apps/api/`, `data/scripts/`, `docs/`, `infra/`), steering files, README.
- **Spec generation:** `docs/tasks.md` (12 tasks), `docs/architecture.md`, `docs/api-contract.md`.
- **Docs:** `docs/infra.md`, `infra/provisioning-status.md` with ARNs/endpoints/teardown commands.
- **Findings script:** `data/scripts/findings.py` — multi-board director analysis, composite scoring, cross-jurisdiction detection, concentration patterns.

## Current

- **Ready to start Task 4 (ETL pipeline).** All dependencies met:
  - Task 1 (schema discovery) done by Agent C — `data/schema.md` available
  - Task 2 (infra) done — Neptune + staging bucket live
  - Golden records strategy confirmed (skip entity resolution, use `entity_golden_records.jsonl` directly)
- Coordination files (`coordination/agent-tasks.md`, `coordination/next-up.md`) do not exist yet; working from `prompts/agent-a-rdp.md` task list.

## What's Next (in order)

1. **Task 4:** Write `data/scripts/etl.py` — transform source JSONL → Neptune bulk-load CSVs using golden records as entity backbone, upload to staging bucket, trigger Neptune Bulk Loader.
2. **Task 6:** Top-20 pre-compute — run composite score query against loaded graph, write `cache/top.json`.
3. **Task 8:** Lambda handlers (`top.ts`, `search.ts`, `person.ts`) + Bedrock smoke test + API Gateway deploy.

## Blockers

None. Ready to proceed with Task 4.
