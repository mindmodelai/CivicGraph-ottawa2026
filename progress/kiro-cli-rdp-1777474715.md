# Kiro-CLI-RDP Progress

Timestamp: 2026-04-29T14:58:18Z

## Done

- **Task 2 (infra provisioning):** Neptune Serverless cluster `civicgraph-graph` (1–8 NCU), staging S3 bucket, IAM roles (neptune-loader + lambda-execution), security groups, DB subnet group, 4 CloudWatch log groups, CDK bootstrap. All resources tagged `Project=civicgraph`. Full report in `infra/provisioning-status.md`.
- **Project scaffold:** Repo structure (`apps/web/`, `apps/api/`, `data/scripts/`, `docs/`, `infra/`), steering files, README.
- **Spec generation:** `docs/tasks.md` (12 tasks), `docs/architecture.md`, `docs/api-contract.md` — iterated through 3 refinement commits.
- **Docs:** `docs/infra.md`, `infra/provisioning-status.md` with ARNs/endpoints/teardown commands, `progress/synopsis.md`.

## Current

- Waiting for coordination files (`coordination/agent-tasks.md`, `coordination/next-up.md`) — not yet created.
- **Next task per agent prompt: Task 4 (ETL)** — JSONL → Neptune bulk-load CSVs → bulk load. Dependencies met (Task 1 schema discovery done by Agent C, Task 2 infra done).

## What's next (in order)

1. **Task 4:** ETL pipeline — read source JSONL from `s3://agency2026-team-2/`, use golden records as entity backbone, generate Neptune bulk-load CSVs, upload to staging bucket, trigger bulk loader.
2. **Task 6:** Top-20 pre-compute — composite score query, write `cache/top.json`.
3. **Task 8:** Lambda handlers (`top.ts`, `search.ts`, `person.ts`) + Bedrock smoke test + API Gateway deploy.

## Blockers

None. Ready to start Task 4.
