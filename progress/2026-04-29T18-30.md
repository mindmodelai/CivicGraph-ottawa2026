# Agent C Progress — 2026-04-29T18:30 ET

## Task 9b: Verify Top 3 Findings Against Source Filings

Pulled raw records from `s3://agency2026-team-2/` and verified each finding in `docs/findings.md` against source data.

### Finding 1: Governance Concentration — GRAND RIVER HEALTH (BN 107579823)
- Queried `cra/govt_funding_by_charity.jsonl` — confirmed $2,130,339,000 total (5 fiscal years, 2020–2024). All provincial, $0 federal.
- Queried `cra/cra_directors.jsonl` — found 10 filing rows but only 2 unique persons (Ron Gagnon CEO + Doug Murray CFO). The "5 directors" in findings.md was a name-variant artifact (case differences, Ron/Ronald, MURRY/MURRAY).
- Finding is actually stronger than stated: $2.1B health authority with only 2 declared officers.
- Verified secondary examples (MonAvenir $1.54B, Cape Breton-Victoria $876M, Parkland $673M).

### Finding 2: Victoria Nalugwa — 55 Boards
- Queried directors file — confirmed exactly 55 distinct BN roots.
- Summed funding across all 55 orgs — confirmed exactly $103,575.
- Confirmed all 55 orgs are BC-based. "Nalugwa" is unique in the dataset (no other person with this surname).

### Finding 3: Cross-Jurisdiction — Yeates, Glenda
- Confirmed 3 boards: Carleton University (ON), Government of Province of Alberta (AB), Canadian Blood Services (ON).
- Federal total: $572,635,389 (claim: $573M — 0.06% off).
- Provincial total: $87,959,602,098 (claim: $88.0B — 0.05% off).
- Cross-checked FONG, ANGELA: 4 boards, $254.9M fed, $84.6B prov — all within 0.04%.

### Result
All 3 findings verified within 0.1%. No replacements needed. Saved full report to `docs/findings-verification.md` with CRA public URLs for each entity.

---

## Task 9c: Build Demo Payload JSON

Created `data/exploration/demo-payload.json` — an array of 3 `PersonDetailResponse` objects (matching the shape defined in `docs/api-contract.md`):

1. **Grand River Health** — org-centric view showing governance concentration pattern, with funding edges by year and provenance records linking to CRA.
2. **Nalugwa, Victoria** — person-centric ego graph showing 10 named orgs + a summary node for the remaining 45, all with SITS_ON edges and CRA source URLs.
3. **Yeates, Glenda** — cross-jurisdiction view showing 3 orgs bridging federal/provincial funding streams, with FUNDED edges from both levels of government.

Each entry includes: `person` summary, full `graph` (nodes + edges), `provenance` array with public URLs, and a `narrative` string suitable for display.

Frontend can `fetch('/data/exploration/demo-payload.json')` as a fallback if the live Neptune/Bedrock API is unreachable during the demo.

---

## Commit

```
9aa6171 feat(data): verified demo findings + fallback graph payloads
```

Pushed to `main`.
